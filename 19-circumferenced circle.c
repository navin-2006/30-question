#include <stdio.h>
#include<math.h>

int main() {
     float a,area,pi=3.14;
     printf("enter the number: ");
     scanf("%f",&a);
     area=pi*pow(a/2,2);
     printf("%f",area);
     
    return 0;
}
